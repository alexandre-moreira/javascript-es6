// Comentários
    /*
        Servem para explicar o código
    */

    // Comentário de uma linha

    /*
        Comentário
        com diversas
        linhas
    */

    // var nome = 'Alexandre'

    /*
        | Comentar um linha de código
        | desativa a mesma. Não deixe
        | linha de código comentadas no arquivo final.
    */

    // Operadores de Atribuição
    // Podem funcionar como formas abreviadas
    var x = 5;
    var y = 10;

    x += y; // x = x + y (15)
    x -= y; // x = x - y (-5)
    x *= y; // x = x * y (50)
    x /= y; // x = x / y (0.5)
    x %= y; // x = x % y (0)
    x **= y; // x = x ** y (9765625)

// Operadore Ternário
    // Abreviação de condicionais com if e else
        var idade = 19;
        var podeBeber = (idade >=18) ? 'Pode Beber' : 'Não pode beber';
        console.log(podeBeber) // Pode beber

    // Condição ? true : false

    /*
        | Geralmente utilizado quando
        | precisamos atribuir um valor
        | para uma variável, dependendo de
        | uma condição.
    */